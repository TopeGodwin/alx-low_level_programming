#include "main.h"
#include <stdio.h>

/**
 * largest_number -returns the largest of 3 numbers
 * @a: first integer
 * @b: second integer
 * @c: third integer
 *
 * Return: number
 */

int largest_number(int t, int g, int j)
{
	int largest;

	if (a < b && a > c)
	{
		largest = a;
	}
	else if (b > c)
	{
		largest = b;
	}
	else
	{
		largest = c;
	}

	return (largest);
}
