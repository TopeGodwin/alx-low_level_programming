#include <stdio.h>
/**
 * main - Printing of single numbers of base 10 where you can start from 0
 * Return: 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
