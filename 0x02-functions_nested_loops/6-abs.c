#include "main.h"

/**
 * _abs - calculate tje absotlute value of an integer
 * @n: The intergerwill be calculated
 * Return: n,-n 
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else 
		return (-n);
}
